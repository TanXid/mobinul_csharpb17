using Task6;

DateTime currentDate = SystemDate.GetCurrentDate();
DateTime currentUtcDate = SystemDate.GetCurrentUtcDate();

Console.WriteLine("Current Date: " + currentDate);
Console.WriteLine("Current UTC Date: " + currentUtcDate);